{
  "name": "json-server",
  "version": "1.0.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "json-server": "^0.17.4"
  },
  "devDependencies": {
    "@types/cors": "^2.8.16",
    "@types/json-server": "^0.14.7",
    "typescript": "^5.2.2"
  }
}
